<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <button>HỌC NGAY</button>
            <ul>
                <li>{{course.level}}</li>
            </ul>
        </div>

        <div class="col-lg-9">
            <h2>{{course.name}}</h2>
            <img class="card-img-top" src="{{course.image}}" alt="{{course.name}}">
            <p>{{course.description}}</p>
        </div>
    </div>
</div>